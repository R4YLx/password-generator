/**
 * Functions for generating parameters
 */

// Generates lowercase
const getLower = () => {};

// Generates uppercase
const getUpper = () => {};

// Generates numbers
const getNumbers = () => {};

// Generates symbols
const getSymbols = () => {};
